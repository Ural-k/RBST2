using UnityEngine;

public class ShowFaceDebug : MonoBehaviour
{
    private void Update()
    {
        //transform.localPosition = transform.parent.GetComponent<Player>().GetInfo.lastFace_ / 2;
    }
}
